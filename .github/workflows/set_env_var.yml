on:
  push:
    branches-ignore:
      - master
name: Set the env vars
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      BUCKET: my-regular-bucket'
    steps:
      - name: set the thing
        run: echo $BUCKET
        with:
          BUCKET: hello
      - name: print
        run: echo $BUCKET

